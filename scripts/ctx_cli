#!/bin/sh
set -eu
SCRIPT_DIR=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
REPO_ROOT=$(CDPATH= cd -- "$SCRIPT_DIR/.." && pwd)
if ! command -v uv >/dev/null 2>&1; then
  echo "ctx_cli wrapper requires 'uv' on PATH" >&2
  exit 127
fi
exec uv run --directory "$REPO_ROOT" ctx_cli "$@"
